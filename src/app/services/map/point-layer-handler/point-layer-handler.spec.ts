import { TestBed } from '@angular/core/testing';

import { PointLayerHandler } from './point-layer-handler';

describe('PointLayerHandler', () => {
  it('should create an instance', () => {
    expect(new PointLayerHandler()).toBeTruthy();
  });
});
